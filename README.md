## Description
A simple game of Tic Tac Toe written in Python to give me some experience with UI

## Prerequisites
Use [uv package manager](https://github.com/astral-sh/uv) for best results, or install requirements with `pip install -r requirements.txt`

## Usage
`uv run ./main.py`, or the equivilent command of your desired Python interpreter